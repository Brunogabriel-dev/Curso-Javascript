// Declare uma variável contendo uma string
var nome = 'Bruno';

// Declare uma variáevel contendo um número dentro de uma string
var ano = '2022';
// Declare uma variável com sua idade
var idade = 26;
// Declare duas variáveis, uma com seu nome
// e outra com seu sobrenome e some as mesmas
var sobrenome = 'Gabriel';
var nomeCompleto = `${nome} ${sobrenome}`;

// Coloque a seguinte frase em uma variavel: It's time
var frase = `It's time`;

// Verifique o tipo da variável que contém o seu nome
var verificarTipoNome = typeof nome;

console.log(verificarTipoNome);